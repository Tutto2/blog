<div class="small-container center-block">
    <p class="front-weight-bold text-uppercase margin-top">Editar usuario</p>
    <% if current_user.avatar.attached? %>
        <%= image_tag url_for(current_user.avatar), height:200 %>
    <%end%>
    <%= form_with(model: current_user, local: true) do |form| %>
        <div class="field">
            <%= form.label :avatar %>
            <%= form.file_field :avatar %>
        </div>
        <div class="field">
            <%= form.submit "Guardar", class:"btn btn-primary" %>
        </div>
    <%end%>

    
    <% if current_user %>
        <li class="nav-item">
            <%= link_to "Mis artículos", user_articles_path(user_id: current_user.id), class: "nav-link" %>
        </li>
    <% end %>
</div>
